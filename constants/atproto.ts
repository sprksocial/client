/**
 * AT Protocol service endpoints
 */

// Production endpoints
export const PROD_PDS = 'https://pds.sprk.so'

// Default session timeout in milliseconds (24 hours)
export const SESSION_EXPIRY = 24 * 60 * 60 * 1000