---
description: 
globs: 
alwaysApply: false
---
You are a senior Dart programmer with experience in the Flutter framework.

Generate code, corrections, and refactorings that comply with the basic principles and nomenclature.

## Dart General Guidelines

### Basic Principles

- Use English for all code and documentation.
- Always declare the type of each variable and function (parameters and return value).
  - Avoid using any.
  - Create necessary types.
- Don't leave blank lines within a function.
- One export per file.

### Nomenclature

- Use PascalCase for classes.
- Use camelCase for variables, functions, and methods.
- Use underscores_case for file and directory names.
- Use UPPERCASE for environment variables.
  - Avoid magic numbers and define constants.
- Start each function with a verb.
- Use verbs for boolean variables. Example: isLoading, hasError, canDelete, etc.
- Use complete words instead of abbreviations and correct spelling.
  - Except for standard abbreviations like API, URL, etc.
  - Except for well-known abbreviations:
    - i, j for loops
    - err for errors
    - ctx for contexts
    - req, res, next for middleware function parameters

### Functions

- In this context, what is understood as a function will also apply to a method.
- Write short functions with a single purpose. Less than 20 instructions.
- Name functions with a verb and something else.
  - If it returns a boolean, use isX or hasX, canX, etc.
  - If it doesn't return anything, use executeX or saveX, etc.
- Avoid nesting blocks by:
  - Early checks and returns.
  - Extraction to utility functions.
- Use higher-order functions (map, filter, reduce, etc.) to avoid function nesting.
  - Use arrow functions for simple functions (less than 3 instructions).
  - Use named functions for non-simple functions.
- Use default parameter values instead of checking for null or undefined.
- Reduce function parameters using RO-RO
  - Use an object to pass multiple parameters.
  - Use an object to return results.
  - Declare necessary types for input arguments and output.
- Use a single level of abstraction.

### Data

- Don't abuse primitive types and encapsulate data in composite types.
- Avoid data validations in functions and use classes with internal validation.
- Prefer immutability for data.
  - Use readonly for data that doesn't change.
  - Use as const for literals that don't change.

### Classes

- Follow SOLID principles.
- Prefer composition over inheritance.
- Declare interfaces to define contracts.
- Write small classes with a single purpose.
  - Less than 200 instructions.
  - Less than 10 public methods.
  - Less than 10 properties.

### Exceptions

- Use exceptions to handle errors you don't expect.
- If you catch an exception, it should be to:
  - Fix an expected problem.
  - Add context.
  - Otherwise, use a global handler.

## Specific to Flutter

### Basic Principles

- Use the following architecture structure:
  ```
  lib/
  ├── main.dart                  # App entry point
  └── src/
      ├── sprk_app.dart          # Main MaterialApp
      ├── core/                  # Shared code across features
      │   ├── config/            # Application-wide configurations
      │   ├── di/                # Dependency injection setup
      │   ├── network/           # ATProto client, API base
      │   ├── routing/           # AutoRoute setup
      │   ├── storage/           # Local storage utilities
      │   │   ├── cache/         # Cache management
      │   │   └── preferences/   # Settings and preferences
      │   ├── theme/             # Theme definitions
      │   ├── auth/              # Authentication system
      │   │   └── data/
      │   │       └── repositories/
      │   ├── feed_algorithms/   # Feed algorithms
      │   ├── l10n/              # Localization (when implemented)
      │   ├── widgets/           # Common widgets
      │   └── utils/             # Shared utilities
      │       └── logging/       # Logging framework
      └── features/              # Feature modules
          └── feature/           # (auth, feed, profile, etc.)
              ├── data/          # Data layer for this feature
              │   ├── repositories/   
              │   └── models/
              ├── providers/     # Riverpod providers
              └── ui/            # UI components
                  ├── pages/
                  └── widgets/
    ```

  Folder structure explanation:
  - `lib/src/core/`: Contains all shared code that's used across multiple features.
    - `config/`: Holds application-wide configurations, such as environment settings, API keys, or feature flags.
    - `di/`: Manages dependency injection setup using GetIt to register and resolve dependencies.
    - `network/`: Handles all network-related functionality, including the ATProto client, API base classes, and repositories. Use only methods included in the ATProto API.
    - `routing/`: Contains the AutoRoute setup, including route definitions, guards, and navigation helpers.
    - `storage/`: Contains utilities for local storage.
      - `cache/`: Cache management with SQL-based implementation, download manager, and cache interfaces.
      - `preferences/`: Settings, storage manager, secure storage, and shared preferences.
    - `theme/`: Defines the application's themes, including colors, typography, and component styles for different modes (e.g., light/dark).
    - `auth/`: Centralized authentication system with repositories for auth operations.
    - `feed_algorithms/`: Contains feed algorithms and related logic.
    - `l10n/`: Manages localization and internationalization (when implemented).
    - `widgets/`: Houses reusable UI components that are used across multiple features.
    - `utils/`: Includes utility functions and helpers.
      - `logging/`: Complete logging framework with different log levels, outputs, and predefined loggers.

  - `lib/src/features/`: Contains feature-specific modules, where each feature is a self-contained unit.
    - Current features: `auth`, `feed`, `profile`, `splash`, `settings`, `comments`, `search`, `messages`, `home`
    - Each feature follows the same structure:
      - `data/`: Handles all data-related concerns for the feature.
      - `providers/`: Contains Riverpod providers that manage the state and business logic.
      - `ui/`: Contains all UI components with `pages/` and `widgets/` subdirectories.

- The backend is using AT Protocol. Any interfacing between frontend and backend will use the AT Protocol API
- Use repository pattern for data persistence
  - Use the cache manager: `CacheManagerInterface` from GetIt
  - Use SQL cache: `SQLCacheInterface` from GetIt
  - Use download manager: `DownloadManagerInterface` from GetIt
- Use Riverpod to manage state
  - see keepAlive if you need to keep the state alive
  - use riverpod annotations
- Use freezed to manage UI states
- Use GetIt to manage dependencies
  - Use singleton for services and repositories
  - Use factory for use cases
  - Use lazy singleton for controllers
- Use AutoRoute to manage routes
  - Use extras to pass data between pages
- Use extensions to manage reusable code
- Use ThemeData to manage themes
- Use AppLocalizations to manage translations (when implemented)
- Use constants to manage constants values
- Use `StorageManager` from GetIt to manage storage
- When a widget tree becomes too deep, it can lead to longer build times and increased memory usage. Flutter needs to traverse the entire tree to render the UI, so a flatter structure improves efficiency
- A flatter widget structure makes it easier to understand and modify the code. Reusable components also facilitate better code organization
- Avoid Nesting Widgets Deeply in Flutter. Deeply nested widgets can negatively impact the readability, maintainability, and performance of your Flutter app. Aim to break down complex widget trees into smaller, reusable components. This not only makes your code cleaner but also enhances the performance by reducing the build complexity
- Deeply nested widgets can make state management more challenging. By keeping the tree shallow, it becomes easier to manage state and pass data between widgets
- Break down large widgets into smaller, focused widgets
- Utilize const constructors wherever possible to reduce rebuilds
- Use Flutter 3.x features.
- Always use withAlpha instead of withOpacity
- Never use relative paths in imports
- Register pages in `lib/src/core/routing/pages.dart` and routes in `lib/src/core/routing/app_router.dart`
- Avoid functions that return widgets
- Use `lib/src/core/utils/logging/` for logging - get `LogService` from GetIt
- Storage keys are always stored at `lib/src/core/storage/preferences/storage_constants.dart`
- Repositories should always have an interface and an implementation (thing_repository.dart, thing_repository_impl.dart)
- For storage, get a `StorageManager` from GetIt
- For cache, get a `CacheManagerInterface` from GetIt
- For auth, get an `AuthRepository` from GetIt
- For API calls, check if `lib/src/core/network/data/repositories/sprk_repository.dart` already has that method in its contract
    - If it does, get `SprkRepository` from GetIt
- Available repositories in service locator:
  - `LogService` - Logging service
  - `SQLCacheInterface` - SQL-based cache
  - `CacheManagerInterface` - Cache manager
  - `DownloadManagerInterface` - Download manager
  - `StorageManager` - Storage management
  - `AuthRepository` - Authentication
  - `SprkRepository` - Main API repository
  - `IdentityRepository` - User identity
  - `ThemeRepository` - Theme management
  - `ActorRepository` - Actor operations
  - `GraphRepository` - Graph operations
  - `SettingsRepository` - Settings management
  - `OnboardingRepository` - Onboarding flow
- Don't forget to add new repositories to the service locator at `lib/src/core/di/service_locator.dart`

 