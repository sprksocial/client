import 'package:material_ui/material_ui.dart';

const double recordingPageFooterHeight = kBottomNavigationBarHeight + 12;
const BorderRadius recordingPagePreviewBorderRadius = BorderRadius.all(
  Radius.circular(20),
);
