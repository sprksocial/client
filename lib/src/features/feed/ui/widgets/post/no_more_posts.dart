import 'package:flutter/material.dart';

class NoMorePosts extends StatelessWidget {
  const NoMorePosts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('No more posts in this feed.'));
  }
}
